# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: pwsh-azcli-func
metadata:
  template: pwsh-azcli-func@0.0.1-beta
services:
  app:
    project: ./src/app
    #language: pwsh
    host: function
